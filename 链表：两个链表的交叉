问题：请写一个程序，找到两个单链表最开始的交叉节点
-------------------------------------------------------------------------------------------------------------
lintcode:https://www.lintcode.com/problem/intersection-of-two-linked-lists/description?_from=ladder&&fromId=6
-------------------------------------------------------------------------------------------------------------
思路：
1.若是要求只有Y型交叉没有X型交叉，那么两个链表的最后一个节点必是相同的；首先遍历两个链表，分别得到链表长度a,b（设此处a>b），并先让长链表的指针走|a-b|步后停止；此时开始同时遍历两个链表，第一个相同的点则为交叉点。否则当指针走到空时，返回null。
2.例外：两个链表都为null/任一个为null
